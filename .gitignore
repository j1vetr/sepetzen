node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# DB dump dosyaları — gizli bilgi içerebilir
sepetzen_dump.sql
sepetzen_dump.dump
*.dump
*.sql
# Şema migration dosyaları sürüm kontrolünde tutulur (deploy için gerekli)
!migrations/*.sql
